# 별빛재 데이터 확인 및 관리 명령어

이 문서는 현재 별빛재 프로젝트의 PostgreSQL 데이터를 빠르게 확인하고 관리하기 위한 실무용 명령 모음입니다.

## 1. 화면과 데이터 연결 구조

| 화면/기능 | PostgreSQL 테이블 | API | 주요 코드 |
|---|---|---|---|
| 홈 → 별빛재의 이야기 | Next.js 정적 데이터 | 없음 | `frontend/data/homeStories.ts` |
| 이야기 데이터 API | `stories` | `GET /api/stories` | `backend/.../story/StoryController.java` |
| 기존 추천 운세 API | `fortunes` | `GET /api/fortunes/recommended` | `backend/.../fortune/FortuneController.java` |
| 백엔드 상태 확인 | 없음 | `GET /api/health` | `backend/.../fortune/FortuneController.java` |

현재 홈 화면의 “별빛재의 이야기”는 서버와 연결하지 않고 `frontend/data/homeStories.ts`를 즉시 렌더링합니다. `stories` 테이블과 API는 백엔드 데이터 확인 및 이후 별도 기능을 위한 것이며 메인페이지에는 자동 반영되지 않습니다.

## 2. 기본 접속 정보

DB 설정은 `backend/.env`에서 관리합니다.

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=byeolbit
DB_USERNAME=byeolbit
DB_PASSWORD=local-secret
```

실제 운영 환경에서는 `DB_PASSWORD`를 별도의 안전한 값으로 변경합니다. `.env` 파일은 Git에 올리지 않습니다.

## 3. PostgreSQL 접속

PowerShell에서 PostgreSQL 명령 경로를 현재 세션에 추가합니다.

```powershell
$env:Path = "C:\Program Files\PostgreSQL\18\bin;$env:Path"
```

DB에 접속합니다. 실행 후 `backend/.env`의 `DB_PASSWORD`를 입력합니다.

```powershell
psql -h localhost -p 5432 -U byeolbit -d byeolbit
```

매번 사용자와 DB를 입력하기 번거롭다면 현재 PowerShell 세션에서만 기본값을 지정할 수 있습니다.

```powershell
$env:PGHOST = "localhost"
$env:PGPORT = "5432"
$env:PGDATABASE = "byeolbit"
$env:PGUSER = "byeolbit"
psql
```

접속 종료:

```sql
\q
```

## 4. DB 및 테이블 상태 확인

PostgreSQL 버전과 현재 접속 정보를 확인합니다.

```sql
SELECT version();
SELECT current_database(), current_user;
```

전체 테이블 목록:

```sql
\dt
```

테이블 구조 확인:

```sql
\d stories
\d fortunes
```

데이터를 세로 보기와 표 보기 사이에서 자동 조정합니다.

```sql
\x auto
```

## 5. 별빛재의 이야기 조회

화면에 표시되는 순서 그대로 조회:

```sql
SELECT
    id,
    slug,
    title,
    subtitle,
    price,
    icon,
    display_order,
    active
FROM stories
ORDER BY display_order, id;
```

현재 화면에 노출되는 데이터만 조회:

```sql
SELECT id, slug, title, subtitle, price, icon
FROM stories
WHERE active = TRUE
ORDER BY display_order, id;
```

특정 이야기 검색:

```sql
SELECT *
FROM stories
WHERE slug = 'nature';
```

제목이나 설명으로 검색:

```sql
SELECT id, slug, title, subtitle
FROM stories
WHERE title ILIKE '%마음%'
   OR subtitle ILIKE '%마음%';
```

## 6. 이야기 수정

제목, 설명, 가격 수정:

```sql
UPDATE stories
SET title = '새로운 이야기',
    subtitle = '새롭게 변경한 이야기 설명',
    price = 1500
WHERE slug = 'nature';
```

아이콘 수정:

```sql
UPDATE stories
SET icon = '★'
WHERE slug = 'nature';
```

수정 결과 확인:

```sql
SELECT * FROM stories WHERE slug = 'nature';
```

## 7. 노출 순서 및 공개 상태 관리

특정 이야기를 첫 번째로 이동:

```sql
UPDATE stories
SET display_order = 1
WHERE slug = 'season';
```

여러 이야기의 순서를 한 번에 정리:

```sql
UPDATE stories
SET display_order = CASE slug
    WHEN 'season' THEN 1
    WHEN 'nature' THEN 2
    WHEN 'love' THEN 3
    WHEN 'work' THEN 4
    WHEN 'together' THEN 5
    WHEN 'question' THEN 6
    ELSE display_order
END;
```

이야기를 삭제하지 않고 화면에서 숨기기:

```sql
UPDATE stories
SET active = FALSE
WHERE slug = 'question';
```

다시 공개하기:

```sql
UPDATE stories
SET active = TRUE
WHERE slug = 'question';
```

운영 데이터는 바로 `DELETE`하기보다 `active = FALSE`로 숨기는 방식을 권장합니다.

## 8. 새 이야기 추가

`slug`는 영문 소문자와 하이픈을 사용하고 중복되지 않게 작성합니다.

```sql
INSERT INTO stories (
    slug,
    title,
    subtitle,
    price,
    icon,
    display_order,
    active
) VALUES (
    'new-story',
    '새로운 별빛',
    '새로운 이야기의 짧은 설명',
    1200,
    '✧',
    7,
    TRUE
);
```

추가 결과 확인:

```sql
SELECT *
FROM stories
WHERE slug = 'new-story';
```

## 9. 안전하게 수정하기

중요한 변경은 트랜잭션 안에서 실행합니다.

```sql
BEGIN;

UPDATE stories
SET price = 1300
WHERE slug IN ('nature', 'love', 'work');

SELECT slug, title, price
FROM stories
ORDER BY display_order;

COMMIT;
```

결과가 잘못됐다면 `COMMIT` 대신 취소합니다.

```sql
ROLLBACK;
```

`UPDATE`나 `DELETE` 전에는 같은 조건으로 먼저 `SELECT`하여 대상을 확인합니다.

```sql
SELECT * FROM stories WHERE slug = 'nature';
```

## 10. API에서 데이터 확인

브라우저에서 바로 확인:

- 이야기 API: `http://localhost:8080/api/stories`
- 기존 운세 API: `http://localhost:8080/api/fortunes/recommended`
- 백엔드 상태: `http://localhost:8080/api/health`

PowerShell에서 이야기 API 확인:

```powershell
Invoke-RestMethod http://localhost:8080/api/stories |
    Format-Table id, slug, title, price, displayOrder, active
```

JSON 원문 확인:

```powershell
Invoke-WebRequest http://localhost:8080/api/stories |
    Select-Object -ExpandProperty Content
```

API 상태 코드만 확인:

```powershell
(Invoke-WebRequest http://localhost:8080/api/health).StatusCode
```

정상이면 `200`이 출력됩니다.

## 11. API 데이터 확인 순서

1. PostgreSQL에서 데이터를 수정합니다.
2. `SELECT`로 수정 결과를 확인합니다.
3. `http://localhost:8080/api/stories`에서 API 응답을 확인합니다.
4. DB 데이터는 현재 메인페이지에 연결되지 않으므로 API에서만 확인합니다.

백엔드가 실행되지 않았다면:

```powershell
cd C:\coke\backend
gradle bootRun
```

프런트엔드가 실행되지 않았다면 새 터미널에서:

```powershell
cd C:\coke
npm.cmd run dev
```

## 12. 실행 중인 포트 확인

```powershell
Get-NetTCPConnection -State Listen |
    Where-Object LocalPort -In 3000, 8080, 5432 |
    Select-Object LocalAddress, LocalPort, OwningProcess
```

프로세스 상세 확인:

```powershell
$processId = 12345
Get-Process -Id $processId
```

- `3000`: Next.js 프런트엔드
- `8080`: Spring Boot 백엔드
- `5432`: PostgreSQL

## 13. 백업과 복원

전체 DB 백업:

```powershell
pg_dump -h localhost -p 5432 -U byeolbit -d byeolbit `
    -F c -f "C:\coke\byeolbit-backup.dump"
```

`stories` 테이블만 SQL로 백업:

```powershell
pg_dump -h localhost -p 5432 -U byeolbit -d byeolbit `
    --table=stories --data-only `
    -f "C:\coke\stories-data.sql"
```

전체 백업 복원:

```powershell
pg_restore -h localhost -p 5432 -U byeolbit -d byeolbit `
    --clean --if-exists "C:\coke\byeolbit-backup.dump"
```

`--clean` 복원은 기존 객체를 삭제한 뒤 다시 만들기 때문에 복원 대상 DB를 반드시 확인합니다.

## 14. 자주 발생하는 문제

### `psql` 명령을 찾지 못하는 경우

```powershell
$env:Path = "C:\Program Files\PostgreSQL\18\bin;$env:Path"
psql --version
```

### API가 404를 반환하는 경우

이전 백엔드가 실행 중일 수 있습니다. 8080 포트의 프로세스를 확인하고 최신 코드로 백엔드를 다시 시작합니다.

```powershell
Get-NetTCPConnection -LocalPort 8080 -State Listen
```

### DB를 수정했는데 메인 화면이 바뀌지 않는 경우

정상 동작입니다. 메인페이지는 서버와 분리되어 있습니다. 메인 콘텐츠를 변경하려면 `frontend/data/homeStories.ts`를 수정한 뒤 Next.js를 다시 빌드하거나 개발 서버에서 새로고침합니다.

### 백엔드 DB 연결 오류

1. PostgreSQL 서비스가 실행 중인지 확인합니다.
2. `backend/.env`의 DB 이름, 사용자, 비밀번호를 확인합니다.
3. 직접 `psql` 접속이 가능한지 확인합니다.

```powershell
Get-Service postgresql-x64-18
psql -h localhost -p 5432 -U byeolbit -d byeolbit
```

## 15. 초기 데이터 파일 주의사항

- 테이블 정의: `backend/src/main/resources/schema.sql`
- 초기 데이터: `backend/src/main/resources/data.sql`
- 초기 데이터는 `ON CONFLICT (slug) DO NOTHING`을 사용합니다.
- 이미 존재하는 `slug`의 데이터는 백엔드를 재시작해도 덮어쓰지 않습니다.
- 운영 중 데이터 변경은 PostgreSQL에서 수행하고, 새 환경의 기본 데이터도 바꿔야 한다면 `data.sql`을 함께 수정합니다.
